```mermaid
flowchart LR
    %% Actor Definition
    플레이어Actor(("플레이어 (Actor)"))

    %% System Boundary
    subgraph DnF_Battle_B_01 ["게임 시스템 (DnF_Battle_B_01)"]
        UC1(["캐릭터생성"])
        UC2(["몬스터공격"])
        UC3(["아이템획득"])
        UC4(["길드가입"])
        UC5(["플레이어체크"])
    end

    %% Relationships
    플레이어Actor --- UC1
    플레이어Actor --- UC2
    플레이어Actor --- UC3
    플레이어Actor --- UC4

    %% Include Relationships
    UC1 -. "«include»" .-> UC5
    UC2 -. "«include»" .-> UC5
    UC3 -. "«include»" .-> UC5
    UC4 -. "«include»" .-> UC5

## 📝 사용사례 명세 및 관계 설명

### 1. 액터 (Actor)
* **플레이어** : 시스템 외부에서 게임 콘텐츠(`캐릭터생성`, `몬스터공격`, `아이템획득`, `길드가입`)를 능동적으로 수행하고 제어하는 주체입니다.

---

### 2. 유스케이스 (Use Case) 명세
| 유스케이스명 | 입력 데이터 | 시스템 처리 및 제어 조건 |
| :--- | :--- | :--- |
| **캐릭터생성** | 캐릭터명, 직업, 레벨 | 플레이어 ID 검증 후 직업별 능력치 초기화 및 **최대 10칸 용량의 빈 인벤토리 자동 결합 생성 (Composition)** |
| **몬스터공격** | 플레이어 ID, 캐릭터 객체 | 직업별 고유 스킬 발동 및 데미지 배율 연산 후 **공격 등급 부여 (S급: 200이상 / A급: 100이상 / B급: 100미만)** |
| **아이템획득** | 플레이어 ID, 아이템명, 타입, 가치 | **인벤토리 최대 수량(10개) 체크** 후 아이템 추가 및 **가치별 등급 부여 (전설: 1000이상 / 희귀: 500이상 / 일반: 500미만)** |
| **길드가입** | 플레이어 ID, 길드명 | 외부 독립 객체인 길드를 탐색하여 **최대 정원(5명) 이내일 경우** 해당 캐릭터를 소속 처리 (Aggregation) |
| **플레이어체크**| 플레이어 ID | 시스템 접근 주체의 고유 식별자가 유효한 값(**"hero"**)인지 검증하는 보안 프로세스 |

---

### 3. 관계 설명 (Relationship)

* **연관 관계 (Association — 실선 `---`)**
  * `플레이어` 액터는 시스템 내 독립적인 주요 비즈니스 기능인 **`캐릭터생성`**, **`몬스터공격`**, **`아이템획득`**, **`길드가입`** 행위를 직접 시작하고 수행할 수 있는 권한을 가집니다.
* **포함 관계 (Include — 점선 화살표 `-. «include» .->`)**
  * 모든 핵심 유스케이스(`캐릭터생성`, `몬스터공격`, `아이템획득`, `길드가입`)는 내부의 정상 로직을 실행하기 전, **반드시 `플레이어체크` 유스케이스를 수행하도록 강제** 설계되어 있습니다.
  * `플레이어체크` 연산 결과가 거짓(`false`, 즉 ID가 "hero"가 아님)일 경우, 하위 핵심 트랜잭션은 모두 안전하게 차단됩니다.
